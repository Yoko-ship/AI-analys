{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "cronSchedule": "10 6 * * *",
    "startCommand": "python collector_financials.py --bank-fx-only",
    "restartPolicyType": "NEVER"
  }
}
